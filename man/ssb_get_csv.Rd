% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hello.R, R/ssb_get_csv.R
\name{ssb_get_csv}
\alias{ssb_get_csv}
\title{Download or load SSB table as CSV with caching}
\usage{
ssb_get_csv(
  table_id,
  folder = "csv/ssb",
  json_folder = "JSON",
  cache_dir = ".cache/ssb",
  language = "no"
)

ssb_get_csv(
  table_id,
  folder = "csv/ssb",
  json_folder = "JSON",
  cache_dir = ".cache/ssb",
  language = "no"
)
}
\arguments{
\item{table_id}{A string representing the SSB table ID, e.g. "12030".}

\item{folder}{Folder path for readable CSV files (default: "csv/ssb").}

\item{json_folder}{Folder path for saved JSON queries (default: "JSON").}

\item{cache_dir}{Hidden folder path for cache storage (default: ".cache/ssb").}

\item{language}{Language code: "no" (default) or "en".}
}
\value{
A data frame (tibble) from the API or CSV

A tibble with data from SSB, either freshly downloaded or cached.
}
\description{
Download or load SSB table as CSV with caching

Download and cache data from Statistics Norway (SSB) using a saved JSON query
}
